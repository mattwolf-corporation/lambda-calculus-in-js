import {test} from "../test.js";

test("church Numbers", assert => {

    assert.equals(true, true );


});

test("kevin", assert => {

    assert.equals(true, true );
    assert.equals(true, true );
    assert.equals(false, true );
    assert.equals(true, true );

});